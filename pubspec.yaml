name: gadget_editor
description: A gadget editor for Ubuntu snaps
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  yaru: ^10.2.0
  yaml: ^3.1.1
  path: ^1.8.3
  file_selector: ^1.0.3
  yaml_writer: ^2.1.0
  yaml_edit: ^2.1.0
  flutter_window_close: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
